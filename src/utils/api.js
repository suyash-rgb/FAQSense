// Utility for API calls
const API_BASE_URL = 'http://localhost:5000/api'; // Placeholder

export const sendMessage = async (message) => {
    // Placeholder for backend call
    return { text: `You said: ${message}. (Mock response)` };
};
